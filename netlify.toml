[build]
  YARN_VERSION = "1.17.3"
  NODE_ENV = "production"
  command = "cd src && npm install --global yarn && yarn run build:client && cd ../server && npm install --global yarn && yarn run build:server"
  publish = "src/dist"

[start]
  command = "cd server && ts-node server/app.ts"
