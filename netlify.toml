[build]
  command = "cd src && yarn install && yarn run build:client && cd ../server && yarn install && yarn run build:server"
  publish = "client/dist"

[start]
  command = "cd server && ts-node server/app.ts"
